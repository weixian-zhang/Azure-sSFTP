 #sSFTP re-inits without restarting on any canges to this config file
sftpPort: 22              #port needs to be exposed at ACI yaml
enableVirusScan: true     #if disabled, files in Staging will remain and not moved
webhooks:
  - name: virusFound
    url: "https://httpbin.org/post"
logDests:
  - kind: file
    props:
      path: "/mnt/ssftp/log"
users:
  - name: "admin"
    password: "22222"
    directory: "agency-z"

  - name: "testuser2"
    password: "tiger"
    directory: "agency-z"

  - name: "testuser3"
    password: "lion"
    directory: "twin-systems"